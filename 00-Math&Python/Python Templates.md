## OI 赛制模板

```text
请你作为具有 ACM/OI 竞赛经验的算法专家，仔细阅读图片或内容中的中的题目描述、输入输出格式及数据范围，并严格按照以下要求为我输出解答：
Python3 标准代码实现：
   - 必须采用 OI 赛制（标准输入输出 `sys.stdin` / `sys.stdout`）。
   - 首行使用注释一句话讲清楚思路
   - 包含常用的快读模板，参考如下：
import sys
input = lambda:sys.stdin.readline().strip()。
   - 尽可能使用 Python 高效的标准库，如 `collections.Counter`, `collections.deque`, `bisect`, `heapq`, `itertools`, `math` 等，保持代码的 Pythonic 和极简。
   - 代码需包含少量简洁核心注释，禁止冗余废话。

请按照以下结构输出：
## Python3 代码
[你的代码]
```



## ACM 赛制模板

